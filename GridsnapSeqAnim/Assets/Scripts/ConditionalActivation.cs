using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public class ConditionalActivation : MonoBehaviour
{
    //[SerializeField]
    //MeshRenderer objectToActivate;

    //[SerializeField]
    //bool startActivated = false;

    //[SerializeField]
    //bool activateOnTrigger = true;

    //enum Conditions { CameraAngle }
    //Conditions condition = Conditions.CameraAngle;

    //[SerializeField]
    //CameraManager.CamAngles triggerAngle;

    //// Start is called before the first frame update
    //void Start()
    //{
    //    objectToActivate.enabled = startActivated;
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    switch(condition)
    //    {
    //        case Conditions.CameraAngle:
    //            if (CameraManager.Instance.angle == triggerAngle)
    //            {
    //                objectToActivate.enabled = activateOnTrigger;
    //            }
    //            else
    //            {
    //                objectToActivate.enabled = !activateOnTrigger;
    //            }
    //            break;
    //    }
    //}
}
