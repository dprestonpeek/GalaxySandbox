using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CamAngle : MonoBehaviour
{
    [SerializeField]
    public CameraManager.CamTargets target = CameraManager.CamTargets.Protag;
    [SerializeField]
    public CameraManager.CamAngles angle = CameraManager.CamAngles.UpperBackRight;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
